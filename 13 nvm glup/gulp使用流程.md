# gulp使用流程
+ 创建工程项目地址

+ 项目根目录右键打开命令行

+ "npm init"进行初始化

+ "npm install gulp --save-dev"添加gulp依赖（安装gulp）

+ 项目根目录下添加一个gulpfile.js文件（gulp主文件，文件名固定）

+ 编写js文件
+ "gulp ++name++"运行编写的操作

+ 安装browerSync

+ 写serve任务

+ 写watch的监视

+ 运行，完成自动更新
